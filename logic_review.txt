- в профиле 
если статус заявки == "Обучение завершено" и отзыва заявки еще нет 
форма /add_review
скрытый инпут с названием request_id и {{r.id}}
обязательное поле ввода для отзыва 
Подтвердить
если отзыв заявки 
Отзыв: {{r.review}}

- в админке
td
форма /admin/change_status
скрытый инпут с названием request_id и {{r.id}}
селект статуса
опция если статус заявки == Новая выбрана value="Новая" 
опция если статус заявки == В процессе выбрана value="В процессе" 
опция если статус заявки == Обучение завершено выбрана value="Обучение завершено" 
Подтвердить

- в мейне
post /admin/change_status(request_id, status):
    Смотрим  из куков пользователь админ
    Если нет
        редиректим к /login    

    подключаемся к БД
        обновляем заявки выставляя статус = ? где id = ?
    редиректим /admin


post /add_review (request_id, review):
    Берем id пользователя из куков
    Если его нет
        редиректим к /login    

    подключаемся к БД
        в cur = "выбираем все из заявок где статус = 'Обучение завершено' и id = ? и нет отзыва (request_id,).fetchone()
        если cur:
            обновить заявки выставляя отзыв = ? где id = ?, (review, request_id)
    редиректим /profile   